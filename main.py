"""
Spotty - A terminal application for downloading songs from Exportify CSV files.

This is the main entry point for running Spotty from the command line.
For the actual implementation, see the spotty package.
"""
from spotty.cli import main

if __name__ == "__main__":
    main()
